<div class="sos-blurb" x-data>
	<p x-show="$store.app.state === 'init'">
		Upload a book in EPUB format to determine if the work was likely written by a human or generated by
		AI. Uploaded files are processed in real-time and are never saved to our servers.
	</p>

	<div x-show="$store.app.state === 'working'" x-cloak class="spinner-container">
		<div class="spinner"></div>
		<p>Analyzing book text, please wait...</p>
	</div>

	<div x-show="$store.app.state === 'result'">
		<p x-show="$store.app.lastResult.exists">This work has been previously submitted. Here were the results.</p>

		<p>Title: <span x-text="$store.app.lastResult.title"></span></p>
		<p>Author: <span x-text="$store.app.lastResult.author"></span></p>
		<p>Result: <span x-text="$store.app.lastResult.result === 1.0 ? 'Likely AI' : 'Likely Human'"></span></p>

		<div x-show="$store.app.lastResult.analysis">
			<p>Avg Sentence Length: <span x-text="$store.app.lastResult.analysis.avg_sentence_length"></span></p>
			<p>Lexical Richness: <span x-text="$store.app.lastResult.analysis.lexical_richness"></span></p>
			<p>Word Count: <span x-text="$store.app.lastResult.analysis.total_word_count"></span></p>
			<p>Total Sentences: <span x-text="$store.app.lastResult.analysis.total_sentences"></span></p>
		</div>
	</div>
</div>